name: Validation recovery final holdout

on:
  push:
    branches:
      - alpha-0.5.1a0-validation-recovery
    paths:
      - scripts/run_validation_recovery_holdout.py
      - model/calibration_validation/validation_recovery_selection_freeze.json
      - model/calibration_validation/validation_recovery_contract.json
      - data/provenance/validation_recovery_split_0.5.1a0.json
      - data/raw/validation_recovery/**
      - src/romania_macro_financial_dynamics/validation_recovery.py
      - .github/workflows/validation-recovery-holdout.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: alpha-0.5.1a0-validation-recovery
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install scientific core
        run: python -m pip install -e '.[test]'
      - name: Evaluate only the selection-frozen eligible holdout
        run: python scripts/run_validation_recovery_holdout.py
      - name: Run recovery tests
        run: python -m pytest -q tests/test_validation_recovery.py
      - name: Persist first-and-only final evaluation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add model/calibration_validation/validation_recovery_holdout.json
          if ! git diff --cached --quiet; then
            git commit -m "science: persist frozen-model validation recovery holdout"
            git push origin HEAD:alpha-0.5.1a0-validation-recovery
          fi
      - name: Upload final evaluation result
        uses: actions/upload-artifact@v4
        with:
          name: validation-recovery-final-holdout
          path: model/calibration_validation/validation_recovery_holdout.json
          if-no-files-found: error
